// Header.js
import React from 'react';
import './Header.css';

function Header() {
  const navbarStyle = {
    backgroundColor: 'transparent', // Set the background color of the navbar to transparent
  };

  return (
    <header>
    
  
    </header>
  );
}

export default Header;
